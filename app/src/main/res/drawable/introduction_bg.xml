<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/white" /> <!-- 設定背景顏色，這裡使用白色 (#FFFFFF) -->
    <corners android:radius="0dp" /> <!-- 設定圓角半徑，這裡使用 8dp -->
    <stroke
        android:color="@color/light_gray"
        android:width="3dp" /> <!-- 設定邊框寬度，這裡使用 1dp -->
</shape>